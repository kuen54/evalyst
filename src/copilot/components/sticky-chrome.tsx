"use client"

/**
 * Re-export shim · Phase 3 #R2-A PR 1/3
 *
 * Glass sticky primitive 已搬到 `src/components/glass/sticky-chrome.tsx`。
 * 本 shim 在 PR 2 bulk migrate import 路径后，PR 3 删除。
 */

export { GlassStickyHeader, GlassStickyFooter } from "@/components/glass/sticky-chrome"
