"use client"

/**
 * Re-export shim · Phase 3 #R2-A PR 1/3
 *
 * Glass segmented primitive 已搬到 `src/components/glass/glass-segmented.tsx`。
 * 本 shim 在 PR 2 bulk migrate import 路径后，PR 3 删除。
 */

export { GlassSegmentedItem } from "@/components/glass/glass-segmented"
